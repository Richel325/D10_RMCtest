{# Get the image file entity from the block content #}
{% set image = elements.content['#block_content'].field_featured_image.entity.field_media_image.entity.uri.value|image_style('max_400_wide') %}

<img src="{{ image }}" alt="" />